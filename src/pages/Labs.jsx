export function Labs() {
  return (
    <div className="app-shell">
      <div className="page-backdrop" />
      <main className="page-layout" style={{ opacity: 1 }}>
        <div className="main-grid" style={{ gridColumn: '1 / -1', padding: '2rem' }}>
          <section className="shell-card" style={{ padding: '2rem', textAlign: 'center' }}>
            <p className="eyebrow first-post-eyebrow">format_thoughts</p>
            <h2>labs</h2>
            <p className="hero-body">coming soon — experiments and prototypes live here.</p>
          </section>
        </div>
      </main>
    </div>
  )
}
