Optional font slot.

This starter uses system fonts by default so it runs immediately.
If you want custom fonts, place them here and wire them in inside src/styles/global.css.
Suggested filenames:
- FormatThoughtsDisplay.woff2
- FormatThoughtsText.woff2
